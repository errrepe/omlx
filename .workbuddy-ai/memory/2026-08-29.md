2026-08-29: Continued ANE feature branch implementation. Fixed tuner async syntax and o_proj calibration accounting, completed opt-in SwiGLU-in-ANE, MoE shared-expert gating, o_proj split/profiler/tuner/UI wiring, benchmark/docs updates, and added regression coverage. Python ANE/support suites pass (561 tests); full non-slow Python suite passes (10,706 passed, 70 skipped, 77 deselected); final affected suites pass (498 tests). Swift ModelSettingsScreenVM selected tests pass (15/15). Rebuilt qwen35_prefill native extension successfully; native symbol/profile smoke test passes. Full Swift test run had an initial ANE DTO expectation failure that was fixed; the remaining failure is the unrelated existing ThemeTests light-window-color assertion.

2026-08-29 (hardware validation): Starting Slice 5 validation on real Apple Silicon. checkpoints at /Volumes/SSD 4TB/AI Models: Jundot/Qwen3.8-27B-oQ4e-mtp (dense, for o_proj+MLP/GDN), Jundot/Qwen3.6-35B-A3B-oQ4e-mtp (MoE, for shared expert), mlx-community/Qwen3.5-2B-bf16 (sanity). Pre-flight passed (ane smoke + 2B harness load). Feature A SwiGLU POC PASSED correctness: ANE accepts 2-conv+silu+mul program, cosine vs exact GPU SwiGLU ~0.9957 at hidden=4096 and 5120; speedup mixed (1.08x @4096, 0.57x @5120). Added --moe-shared-expert flag to benchmarks/qwen35_ane_prefill_bench.py; created benchmarks/ane_tuner_dryrun.py for offline o_proj tuner dimension check. Dense 27B campaign (gpu / dual / dual+oproj) running in background; MoE + tuner dry-run deferred until it frees memory.

2026-08-29 (validation DONE + committed): Feature B (dense 27B, 4096 tok) — GPU 99.60; dual no-oproj 132.49 (+33.0%); dual+o_proj 4L 134.64 (+1.6%), 8L 133.81 (+1.0%), 16L 57.62 (ANE bank overflow regression, ~14.5 GiB/128-proc limit). Fixed real-model o_proj staging tuple-unpack bug in qwen35_ane_prefill.py (prepared_oprojs = (linear, state) tuples). Feature C (35B-A3B MoE, 2048 tok) — GPU 762.97; dual+shared-expert 802.63 (+5.2%), 40 shared experts + 30 GDN offloaded, routed switch_mlp stays GPU. Tuner dry-run PASS: 101 points, o_proj slot 6, oproj_enabled=True, fraction=0.6, 10.7ms. Filled results table in docs/experimental/qwen35_ane_prefill.md (incl. honest limitations: no e2e greedy cosine/top-1 parity probe this pass; Feature A speed unproven at large hidden). Committed everything as 83d285d2 on feat/ane-oproj-moe-swiglu (39 files, +3463/-58). All extensions remain default-off (setting flag + per-model allow).

2026-08-29 (build/test env): webUI sobe ok com `omlx serve --model-dir "/Volumes/SSD 4TB/AI Models" --host 127.0.0.1 --port 8088` (dashboard em /admin/dashboard, 401=precisa admin key; descobriu 14 modelos). App macOS NÃO compila headless: falha em dependência SPM NetworkImage com `swift-plugin-server produced malformed response` ao resolver macro SwiftUIMacros.StateMacro (@State) — bug de toolchain do Xcode-beta 27.0 (só ele instalado; não há Xcode estável). Ad-hoc signing (CODE_SIGN_IDENTITY=-) passa do signing mas trava na compilação da macro do pacote; .app nem é gerado. Contorno: abrir oMLX.xcodeproj no Xcode-beta e Cmd+B (host de macros da IDE pode resolver); ou instalar Xcode estável. Novas opções ANE já wireadas em ModelSettingsScreen.swift (~1158-1487: SwiGLU on ANE 1436, MoE Shared Expert on ANE 1451, o_proj on ANE 1460) e dashboard.js (47-51).

2026-08-29 (webUI fix): As 3 novas extensões ANE NÃO apareciam na webUI porque faltava adicionar os controles ao template `_modal_model_settings.html` (só estavam no dashboard.js + backend + app macOS). Adicionadas agora: (1) en.json + pt-BR.json ganharam 9 chaves i18n (swiglu/moe_shared/oproj + hints), todas com fallback p/ inglês em outros locales; (2) template ganhou 3 toggles dentro do bloco `x-show=qwen35_ane_prefill_enabled` após a seção GDN — SwiGLU on ANE (desabilitado se dual ANE off, como no app), MoE Shared Expert on ANE, o_proj on ANE (revela fração 0.05-0.90 + limite de camadas 1-256). Servidor reiniciado em background (nnXiO4) e validado por login (key `qwen`) + grep no HTML ao vivo: os 3 controles renderizam. CHECKLIST p/ nova opção de model setting: dashboard.js (data model defaults + lista de chaves + validate + apply) + template HTML + i18n (en.json obrigatório, cai p/ inglês) + app macOS. API key do dashboard = `qwen` (settings.json api_key).

2026-08-29 (bench ANE controls — BOTH UIs): User pediu opções de bench p/ testar as 3 extensões ANE na webUI (_bench.html) E no app Swift (ThroughputBenchScreen). Implementado: (A) BACKEND override per-run em benchmark.py — BenchmarkRequest ganhou 6 campos Optional (qwen35_ane_prefill_enabled/swiglu_in_ane/moe_shared_expert/oproj/oproj_fraction/oproj_max_layers); _apply_bench_ane_override(sm,request) faz merge transitivo via sm.set_settings ANTES do get_engine e o finally restaura o original (salvo settings nunca mudam). Helpers testados em unit (apply→True/True/0.5, restore→False/False). (B) webUI _bench.html: grupo "Qwen ANE Prefill (this run)" dentro de Advanced options, gated a isQwen35AnePrefillModel(benchModelId); master "customize" toggle (quando off = zero-code path/herda saved), revela master enable + SwiGLU/MoE Shared/o_proj(+fração/camadas). dashboard.js: estado benchAne* + seedBenchAneFromSaved() (lê model.settings) + payload só envia quando benchAneCustomize. i18n: bench.config.ane_* em en.json+pt-BR.json. (C) Swift: BenchStartRequest ganhou os 6 campos Optional (snake_case encoder); ThroughputBenchScreenVM ganhou estado + seedAneFromSaved() + pendingFeatureFlags agora inclui Qwen ANE flags (config efetiva); ThroughputBenchScreen.ConfigurationSection ganhou os toggles gated a isQwenAneModel. Validado webUI ao vivo (login qwen + grep: bench.config.ane_prefill_group renderiza; POST /admin/api/bench/start aceita os 6 campos → 200). Swift NÃO compila aqui (Xcode-beta macro bug). Servidor webUI rodando em background (task yhyqt5, porta 8088).

2026-08-29 (eligibility gate + node-id corruption FIX): User decidiu elegibilidade = Qwen3.5/3.6/3.8 **OU** qlq modelo cujo saved Model Settings já habilita ANE prefill. Aplicado em ambas as UIs: webUI x-show mudou de `isQwen35AnePrefillModel(models.find(m => m.id === benchModelId))` p/ `benchAneEligible(benchModelId)` (dashboard.js já tinha benchAneEligible: true se isQwen35AnePrefillModel(m) OU !!s.qwen35_ane_prefill_enabled); Swift isQwenAneModel virou `.map { isQwen35AnePrefillModel($0) || ($0.settings?.qwen35AnePrefillEnabled ?? false) }`. **BUG CRÍTICO DESCOBERTO**: o template _bench.html estava CORROMPIDO com 297 atributos `data-page-node-id="..."` injetados (inclusive DENTRO de expressões Alpine, ex. `m => m.id` virou `m = data-page-node-id="X"> m.id`, quebrando a página). Origem: uma camada de injeção de node-id do editor visual do WorkBuddy (shim sitecustomize.py) que RE-SERIALIZA/re-injeta em arquivos .html do projeto a cada write/watch — writes via Python (Edit/write_text/os.replace) são interceptados e re-artifactados; `os.replace` entre /tmp e volume dá EXDEV. **WORKAROUND QUE FUNCIONA**: (1) ler o arquivo, remover ` data-page-node-id="[^"]*"` via regex python (conserta setas e remove atributos); (2) escrever limpo em /tmp (não é vigiado); (3) `cp /tmp/clean.html <destino>` (cp é binário C, NÃO passa pelo shim → grava bytes reais limpos); (4) commitar blob limpo com `git hash-object -w /tmp/clean.html` + `git update-index --add --cacheinfo 100644 <blob> <path>` (pois `git add` do working tree artifactado stagingia sujo). `git` (C) e `cp` leem/gravam disco real; só Python open é shimado. Só _bench.html é afetado (grep repo inteiro confirmou). Committed tudo como e413576f (10 files, +880/-363); blob de _bench.html tem 0 artifacts e contém benchAneEligible. **NOTA**: o working tree _bench.html continua aparecendo `M` depois do commit pq o watcher re-artifacta; na máquina do user (sem essa camada) o checkout é limpo. Não consegui rodar o hardware bench ao vivo no sandbox: a página servida fica quebrada pela injeção e não há modelo carregado aqui. Próximo passo (escolha do user) = hardware test no app Swift compilado no Xcode.

2026-08-29 (sweep feature — webUI DONE + committed dd728833): User clarificou que a ideia não era só override per-run, mas um SWEEP/matrix: a pessoa marca várias opções ANE e o bench roda cada combo + baseline para dar referência da melhor config. Implementado na webUI: (1) dashboard.js runSweep() + helpers (_waitForBenchComplete, _extractSweepMetrics, _sweepComboName) — monta combos [Baseline(GPU) + cada feature sozinha + todas juntas], roda cada um via startBenchmark() (reusa override per-run + auto-revert → saved settings nunca mudam), extrai pp/generation TPS do maior pp, computa speedup vs baseline e destaca best (maior pp TPS). benchSingleResults é resetado a cada startBenchmark (linha 9457) então cada combo lê só seus resultados. (2) _bench.html: bloco 'ANE Prefill Sweep' gated a benchAneEligible, com 3 checkboxes (SwiGLU/MoE Shared/o_proj) + botão Run sweep (progresso N/total + spinner) + tabela comparativa (Config/PP tok/s/Gen tok/s/Speedup, linha best em verde). (3) i18n en.json+pt-BR.json: bench.config.ane_sweep_* + js.error.sweep_external_unsupported. (4) benchmark.py: REMOVIDO campo morto bench_sweep_features (sweep é frontend-only, não precisa de loop no backend). Servidor webUI validado ao vivo (login key `qwen`): HTML servido tem o bloco sweep, 0 node-ids, e o dashboard.js servido tem runSweep. Committed dd728833 (4 files, +270, 0 node-ids no blob _bench.html). **Swift sweep parity PENDENTE** — bloqueada pelo mesmo bug de macro do Xcode-beta (app não compila aqui); ThroughputBenchScreen.swift ainda não tem UI de sweep, mas o backend de override (6 campos) já está pronto para reuso.

2026-08-29 (CORREÇÃO CRÍTICA — quem corrompe o _bench.html): Não é o usuário que abre o arquivo. O corruptor é o **preview do harness do WorkBuddy** — quando `present_files` é chamado com URL `http://127.0.0.1:8088/...`, o painel de browser embutido do harness abre a página, mapeia de volta pro `_bench.html` fonte e o editor visual RE-SERIALIZA o arquivo (converte markup estático em loop Jinja `{% for %}`, injeta `data-page-node-id` DENTRO de arrow functions Alpine, etc.). A cada preview, o template quebra de novo. **Regra**: NUNCA usar `present_files` com URL localhost do dashboard. Verificar SÓ via `curl` (server-side, não toca o editor → não re-injeta). Cards de artifact para `.js`/`.json`/`.py` são seguros (não vigiados); NÃO apresentar `_bench.html` como artifact. Restore se corromper: `git show HEAD:omlx/admin/templates/dashboard/_bench.html > /tmp/clean.html && cp /tmp/clean.html omlx/admin/templates/dashboard/_bench.html`. Skill omlx-html-nodeid-cleanup ATUALIZADO com esse insight + o bug do `nohup &` (reapado pelo cleanup do tool → usar run_in_background:true do Bash).

2026-08-29 (i18n caching gotcha): o server lê `omlx/admin/i18n/*.json` no IMPORT (`_en_locale = json.loads((_i18n_dir/"en.json").read_text())` em routes.py ~1169) e embute no `locale_json` (global Jinja). Editar o i18n no disco NÃO aparece na página servida até **restartar `omlx serve`**. Sintoma: página mostra keys brutos (`bench.config.ane_sweep_swiglu`) em vez de strings, keys antigos still render. Fix: restart server; verificar via curl que o HTML servido embute os novos keys em `locale_json`. Restart NÃO usa `nohup &` (morre) — usar Bash run_in_background:true com `omlx serve --model-dir "/Volumes/SSD 4TB/AI Models" --host 127.0.0.1 --port 8088`. Confirmado: após restart, served HTML tem 0 node-ids e os 12 keys sweep em locale_json → t() resolve strings corretas.

2026-08-29 (sweep N-runs averaging — DONE + committed ecd5b136): User pediu média de N runs por combinação (margem de erro térmico/scheduling/ANE cache). Implementado: cada combo roda N vezes (default 3, input 1–5 no UI), runSweep() coleta pp/gen TPS de cada run, calcula MEAN + min/max; speedup e "Best" usam a média; progresso mostra "combo · run r/N"; tabela mostra média + range "(min–max)" quando N>1. dashboard.js benchSweepRuns:3; _bench.html input + células de tabela; i18n bench.config.ane_sweep_runs + hint (en/pt-BR). Verificado via curl: served HTML tem runs input + ane_sweep_runs em locale_json + 0 node-ids. **GOTCHA restart**: `pkill -f "omlx serve"` NÃO matou o server (o processo vira `python3.1 /Users/errepe/.omlx/bin/omlx serve ...` via `exec`, e o pkill aparentemente não casou / respawnou). Teve que `kill <pid>` direto do listener python em 8088 (lsof -ti tcp:8088) e subir novo via run_in_background:true. Sempre confirmar via curl que a i18n nova aparece (ane_sweep_runs) antes de declarar pronto.

2026-08-29 (sweep visual-pollution fix — DONE + committed ecdcc090): Durante o sweep, runSweep() seta benchAneCustomize=true para dirigir o override per-run, o que revelava os checkboxes "Qwen ANE Prefill (this run)" como poluição visual. Fix: x-show do bloco per-run mudou de `benchAneCustomize` para `benchAneCustomize && !benchSweepRunning` (esconde durante o sweep, reaparece antes/depois). Commitado blob limpo via `git hash-object -w /tmp/clean_bench3.html` + `git update-index --cacheinfo` (0 node-ids). Verificado via curl com login key `qwen` (auto-login endpoint): served HTML em /admin/dashboard tem o x-show novo na linha 8307, 0 node-ids, SEM restart (Jinja re-renderiza por request).

2026-08-29 (Fase 2 — min-PP gate DONE + committed 396b09d7): User quis produção = ativar modos ANE só acima de um threshold de PP. Implementado como setting único p/ os 3 modos (SwiGLU/MoE Shared/o_proj) `qwen35_ane_prefill_min_pp_tokens` (0 = sempre ligado). Design do gate em runtime: ANE é compilado UMA vez no load (fixed-shape), então NÃO dá p/ recompilar por request. Solução barata e reversível: `set_qwen35_ane_prefill_skip(model, skip)` em qwen35_ane_prefill.py faz soft-latch dos `_omlx_ane_*_failed` per-module (mantém os banks compilados; dispatch cai p/ GPU). Helper puro `ane_prefill_should_engage(num_tokens, threshold)` em omlx/ane_prefill_gate.py (sem MLX, testável). batched.py `_apply_ane_prefill_min_pp_gate(prompt)` chamado em generate()/stream_generate() antes do prefill, idempotente, roda no MLX executor thread, NUNCA muta saved settings. Verificado ao vivo (curl login key qwen): modal tem input Min PP, i18n qwen_ane_min_pp em locale_json, 0 node-ids. **BUG PRÉ-EXISTENTE DESCOBERTO E CORRIGIDO**: o save payload do modal (dashboard.js ~8337) e o buildModelSettingsState OMITIAM os toggles de extensão ANE (swiglu/moe/oproj/oproj_fraction/oproj_max_layers) → eram ligáveis na UI mas NUNCA persistiam no backend. Adicionados tanto no seed quanto no payload (backend já os aceitava em routes.py). Testes: tests/test_qwen35_ane_min_pp_gate.py (6 passando, inclui latch-toggle real via fake model). **VALIDAÇÃO DE HARDWARE PENDENTE**: o gate em si (engage/desengage por tamanho de prompt em modelo real) não foi testado em hardware aqui (sem modelo carregado no sandbox) — flags p/ user rodar sweep multi-PP na máquina dele. Swift parity (Min PP em ThroughputBenchScreen + SettingsScreen) AINDA bloqueada pelo bug de macro do Xcode-beta.
